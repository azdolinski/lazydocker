name: Update docker-compose binary

on:
  workflow_dispatch:
  workflow_call:
    outputs:
      version:
        description: "Resolved docker-compose version"
        value: ${{ jobs.update.outputs.version }}

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.detect_version.outputs.version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download and store latest docker-compose
        run: bash ./scripts/update_docker_compose.sh

      - name: Commit and push changes
        run: |
          git add -A binaries

          if git diff --cached --quiet; then
            echo "No changes in binaries directory."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: update docker-compose binary"
          git pull --no-rebase origin main
          git push

      - name: Detect resolved version
        id: detect_version
        if: always()
        run: |
          version="$(find binaries -maxdepth 1 -type f -name 'docker-compose.v*' ! -name '*.md5' -printf '%f\n' 2>/dev/null | sed 's#^docker-compose\.##' | sort -V | tail -n 1)"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"
